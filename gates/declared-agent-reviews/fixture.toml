tools = ["rule-tool-contracts", "subagent-review-stage"]
violates = "project_model 里一个 [[agent_reviews]] 都没有 —— 审查链没有起点,却产出 0 份材料后退 0"
strip = [{ file = ".ai-config/project_model.toml", table = "agent_reviews" }]
