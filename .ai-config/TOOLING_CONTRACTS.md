# Tooling Contracts

This file is the human-facing index for repository mechanical gates.

The source of truth is `.ai-config/config/tooling.registry.toml`; `tools/check.py` is the unified entrypoint; `.ai-config/tools/check_rule_tool_contracts.py` verifies the wiring.
